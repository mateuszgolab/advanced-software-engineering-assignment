#include "gtest/gtest.h"
#include "Factory.h"
#include "Order.h"

TEST(manufactureProductTest, positiveTest)
{
	/*Factory factory;
	Order order(1, 2, 100, 1, 3);
	factory.setProduct(order.getProduct(0), order.getID());
	EXPECT_EQ(1.0, factory.manufacture());*/
	
}
	
	